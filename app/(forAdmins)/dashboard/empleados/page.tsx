export default function EmpleadosPage() {
  return (
    <section className="mx-auto max-w-6xl text-foreground">
      <h1 className="font-[family-name:var(--font-display-family)] text-3xl font-semibold">
        Empleados y roles
      </h1>
      <p className="mt-3 text-foreground/70">
        Gestiona usuarios del sistema, roles (admin, manager, lead, tecnico) y permisos por modulo.
      </p>
    </section>
  )
}
